# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.log

# Virtual env
.venv/
venv/

# Flask instance / SQLite
instance/
*.db

# OAuth / secrets (ajuste os nomes conforme os seus arquivos)
token.json
credentials.json
client_secret*.json
*.env

# VS Code
.vscode/

# Claude Code
.claude/

# Backups / cópias do banco (contêm dados reais — CPF, e-mail) — NUNCA versionar
backups/
*.dump
*.sql.gz
.railway-db-url
.local-db-url

# Ferramentas locais de banco (caminhos/setup pessoais) — remova esta linha para versionar
/scripts/db/

# Agente auditor financeiro (feature 221): token e artefatos de rodada — NUNCA versionar
# (runs/ contém comprovantes baixados; data/ tem a memória com extrações)
.audit-agent-token
/scripts/auditor/data/
/scripts/auditor/runs/

# Exportações do Kommo CRM (dados reais de clientes — nome/telefone) — NUNCA versionar (feature 094)
kommo_export_*.csv

# Scripts de uso único / debug / ajuste local
zerar_import.py
debug_birth.py
backfill_birth_dates.py
fix_figurino_photos.py
migrate_figurinos.py
reset_beta.py
setup_skills.sh
explicacao
clean_events.py

# Projeto auxiliar local com credencial de produção em texto puro (Manto_Sales/config.py traz a
# chave do Google Maps). Nunca foi versionado — esta linha existe para que um `git add -A`
# distraído não o publique no GitHub.
Manto_Sales/

# Auditor de marketing (feature 256): token e artefatos de rodada — NUNCA versionar
# (inbox/processed guardam exports brutos; runs/ tem relatórios; data/ a memória local)
.marketing-agent-token
/scripts/marketing/inbox/
/scripts/marketing/processed/
/scripts/marketing/runs/
/scripts/marketing/data/

# Relatorio de anexos orfaos (hotfix 257): retrato do volume com nomes de cliente,
# gerado sob demanda — nao versionar.
/specs/257-hotfix-anexos-persistencia/orfaos_*.md
